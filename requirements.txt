# StepBeyond v2.0 - Memory-Optimized for Render Free Tier
# Lightweight dependencies to fit in 512MB memory limit

# Core FastAPI framework
fastapi==0.115.0
uvicorn[standard]==0.27.0
python-multipart==0.0.7
python-dotenv==1.0.1

# Qdrant vector database client (lightweight)
qdrant-client==1.7.3

# Document processing (minimal)
PyPDF2==3.0.1
python-docx==1.1.0

# Lightweight text embeddings (instead of sentence-transformers)
# Using OpenAI's text-embedding-ada-002 via API instead of local models

# HTTP client for API calls
httpx==0.26.0

# Text processing
nltk==3.8.1

# Core utilities
pydantic==2.5.3
numpy==1.24.4

# Logging
loguru==0.7.2

# Basic text processing
scikit-learn==1.3.2
