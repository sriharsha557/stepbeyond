# StepBeyond v2.0 - FastAPI + Qdrant Dependencies
# Tested combination for successful Render deployment - January 2025

# Core FastAPI framework
fastapi==0.115.0
uvicorn[standard]==0.27.0
python-multipart==0.0.7
python-dotenv==1.0.1

# Qdrant vector database client
qdrant-client==1.7.3

# Document processing
PyPDF2==3.0.1
python-docx==1.1.0

# Text processing and embeddings
sentence-transformers==2.3.1

# HTTP client for API calls (Groq integration)
httpx==0.26.0

# Text splitting (minimal langchain)
langchain-text-splitters==0.0.1

# Core utilities
pydantic==2.5.3
numpy==1.24.4

# Logging
loguru==0.7.2

# NOTE: Removed dependencies that caused conflicts:
# - streamlit (not needed for FastAPI backend)
# - crewai (replaced with direct API calls)
# - transformers (pulled by sentence-transformers)
# - groq (using httpx for direct API calls)
# - langchain-core (minimal usage)
# - pandas (not needed for core functionality)
# - requests (using httpx instead)
